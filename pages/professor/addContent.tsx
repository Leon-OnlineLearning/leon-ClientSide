import AddContent from "../../components/add-content/addContent";

export default function AddContentPage() {
    return (
        <AddContent courseId="TODO replace this placeholder with the course id" />
    )
}